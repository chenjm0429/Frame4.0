<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorControlNormal</item>

        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 图片全屏-->
    <style name="AppTheme.ImgFullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!--全透明效果-->
    </style>

    <style name="AppTheme.FullScreen">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <!-- Toolbar菜单文字样式  -->
    <style name="AppTheme.ToolbarTheme" parent="AppTheme.AppBarOverlay">
        <item name="actionMenuTextColor">@color/font_white_color</item> <!--颜色-->
        <item name="android:textSize">16sp</item> <!--字体大小-->
    </style>

    <!--Toolbar菜单样式-->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- 菜单每栏设置 -->
        <item name="actionOverflowMenuStyle">@style/AppTheme.PopupOverlay.StackOverflow</item>
        <!-- 菜单分割线设置 -->
        <item name="android:dropDownListViewStyle">@style/AppTheme.DivideLine.MenuLine</item>
        <!-- 菜单item字体颜色 -->
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay.StackOverflow" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">5dip</item>
        <item name="android:popupBackground">?attr/colorPrimary</item>
        <item name="android:dropDownHorizontalOffset">0dip</item>
    </style>

    <style name="AppTheme.DivideLine.MenuLine" parent="Base.Widget.AppCompat.ListView.DropDown">
        <item name="android:divider">#DEDEDE</item>
        <item name="android:dividerHeight">1dp</item>
    </style>

    <!-- 头部布局风格 -->
    <style name="app_title_layout_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/app_title_height</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:paddingLeft">12dip</item>
        <item name="android:paddingRight">12dip</item>
    </style>

    <!-- 头部文字风格 -->
    <style name="app_title_textview_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">end</item>
    </style>

    <!-- 侧边栏布局样式 -->
    <style name="sidebar_layout_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingRight">15dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <!-- 侧边栏文本框样式 -->
    <style name="sidebar_textview_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#5a5a5a</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

    <!-- dialog风格，从下向上弹出，从上向下收起 -->
    <style name="other_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/PopupWindowAnimationDownToUp</item>
    </style>

    <!--照片选择器样式-->
    <style name="picture_select_style" parent="@style/Matisse.Zhihu">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>
